<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Edit Stakeholder - <%= project.pname %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body>

  <div class="container mt-4">
    <h3 class="mb-4">Edit Stakeholder - <%= project.pname %></h3>

    <form action="/project/<%= project._id %>/stakeholders/edit/<%= index %>" method="POST">
      <div class="mb-3">
        <label>Name</label>
        <input type="text" class="form-control" name="name" value="<%= stakeholder.name %>" required />
      </div>

      <div class="mb-3">
        <label>Contact Number</label>
        <input type="text" class="form-control" name="contact" value="<%= stakeholder.contact %>" required />
      </div>

      <div class="mb-3">
        <label>Email ID</label>
        <input type="email" class="form-control" name="email" value="<%= stakeholder.email %>" required />
      </div>

      <div class="mb-3">
        <label>Stakeholder Type</label>
        <select class="form-select" name="type" required>
          <option value="">Select Type</option>
          <option value="HR Coordinator" <%= stakeholder.type === 'HR Coordinator' ? 'selected' : '' %>>HR Coordinator</option>
          <option value="Manager" <%= stakeholder.type === 'Manager' ? 'selected' : '' %>>Manager</option>
          <option value="Client" <%= stakeholder.type === 'Client' ? 'selected' : '' %>>Client</option>
        </select>
      </div>

      <div class="mb-3">
        <label>Address</label>
        <textarea class="form-control" name="address" rows="3"><%= stakeholder.address %></textarea>
      </div>

      <button type="submit" class="btn btn-success">Update</button>
      <a href="/project/<%= project._id %>/stakeholders" class="btn btn-secondary">Cancel</a>
    </form>
  </div>
</body>
</html>

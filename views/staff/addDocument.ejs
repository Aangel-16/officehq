<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Document</title>
  <link rel="stylesheet" href="/css/style.css"> <!-- optional -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <style>
    .form-container {
      max-width: 600px;
      margin: 30px auto;
      padding: 30px;
      border: 1px solid #ddd;
      border-radius: 15px;
    }
    .form-control, .form-select {
      border-radius: 15px;
    }
    .btn-save {
      background-color: #009bbf;
      color: white;
      border-radius: 30px;
      padding: 8px 25px;
      border: none;
    }
    .btn-cancel {
      background-color: white;
      color: black;
      border: 1px solid #ccc;
      border-radius: 30px;
      padding: 8px 25px;
    }
    .upload-box {
      border: 2px dashed #ccc;
      border-radius: 10px;
      padding: 30px;
      text-align: center;
      color: #999;
      cursor: pointer;
    }
    .upload-box:hover {
      background-color: #f9f9f9;
    }
  </style>
</head>
<body>

  <div class="container form-container">
    <h3 class="mb-4 text-primary">Add Document</h3>
    
   <form action="/staff/documents/<%= staffId %>" method="POST" enctype="multipart/form-data">
      <div class="row mb-3">
        <div class="col">
          <label class="form-label">Category <span class="text-danger">*</span></label>
          <select name="category" class="form-select" required>
            <option value="">Select</option>
            <option value="ID">ID</option>
            <option value="Finance">Finance</option>
            <option value="Work">Work</option>
            <option value="Other">Other</option>
          </select>
        </div>
        <div class="col">
          <label class="form-label">Document Type <span class="text-danger">*</span></label>
          <select name="type" class="form-select" required>
            <option value="">Select</option>
            <option value="Aadhaar">Aadhaar</option>
            <option value="License">License</option>
            <option value="Passport">Passport</option>
            <option value="Credit Card">Credit Card</option>
          </select>
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="3" placeholder="Enter description..."></textarea>
      </div>

      <div class="mb-4">
        <label class="form-label">Upload</label>
        <div class="upload-box">
          <input type="file" name="document" class="form-control" style="border: none;" required>
          <p class="mt-2">Drag & Drop files or <span class="text-primary">Browse</span></p>
        </div>
      </div>

      

      <div class="d-flex justify-content-end gap-2">
        <a href="/staff/<%= staffId %>" class="btn btn-cancel">Cancel</a>
        <button type="submit" class="btn btn-save">Save</button>
      </div>
    </form>
  </div>

</body>
</html>
